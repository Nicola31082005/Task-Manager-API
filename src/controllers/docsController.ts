import { Router, Request, Response } from "express";

const docsController = Router();

docsController.get('/', async (req: Request, res: Response) => {

    try {
        
    } catch (error) {
        
    }

})



export default docsController
